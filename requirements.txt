# Synced from pyproject.toml — keep in sync with [project.dependencies] + [project.optional-dependencies].
# This file = core deps + the optional `ollama` extra + the dev extra, so
# `pip install -r requirements.txt` equals `pip install -e ".[ollama,dev]"`.
# Reproducible install (exact pins, CI-enforced): `pip install -c constraints-dev.txt -r requirements.txt`
# (pins live in constraints-dev.txt; the `audit` CI job fails on drift or known CVEs via pip-audit).
# The Ollama Python SDK is an OPTIONAL provider dependency: a non-Ollama
# provider (models.provider: opencode_go / chatgpt) imports successfully
# without it (tools/model_router must import cleanly with no ollama package).
# The Playwright SDK is an OPTIONAL browser dependency (NOT installed here):
# browser execution needs `pip install -e ".[browser]"` plus the Chromium
# runtime (`python -m playwright install chromium`) for host-side dev, or the
# sandbox browser worker image for real runs (docker/sandbox/Dockerfile.browser).
# playwright>=1.44  # browser extra only — uncomment/install explicitly.
 ollama>=0.6.1
httpx>=0.27.0
mcp>=1.27.0,<3.0.0
PyYAML>=6.0.2
uvicorn>=0.30.0
starlette>=0.37.0
fastapi>=0.110.0
websockets>=12.0
questionary>=2.0.0
numpy>=1.24.0
cryptography>=42.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-xdist>=3.0.0
pytest-timeout>=2.0.0
coverage>=7.3.0
ruff>=0.6.0
mypy>=2.0
build>=1.0
twine>=5.0