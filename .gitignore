# Built distribution artifacts — never commit; CI builds from a clean tree
# (package job: python -m build + twine check) and uploads dist/* as a
# release artifact.
dist/
__pycache__/
*.py[cod]
*$py.class

.venv/
venv/
env/
node_modules/
webui/node_modules/
webui/dist/
# Vendored openai-oauth ChatGPT-provider proxy (built locally via bun install).
oauth/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

*.log
*.db
*.sqlite
*.sqlite3

reports/
exploit_workspace/
research_workspace/
test_workspace*/
swarm_workspace/
*.egg-info/

_analysis_output.json
_cycles_output.json
_analyze_*.py
main-backup.py
main.py.truncated
test_spinner*.log

# Local secrets / editor-local config (never commit)
.webui_secret_key
opencode.json
secr.json
.env
.env.*
!.env.example

# Agent / editor-internal state (never commit)
.claude/

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/

# Log directories
logs/
/docs/phase1-audit
/build
demo/dist/
demo/*.tsbuildinfo
webui/*.tsbuildinfo

# Scratch / accidental test artifacts
x/
/web
